---
"@open-codesign/desktop": patch
---

Preserve URL encoding for local asset paths in exported ZIPs so filenames containing spaces, quotes, or parentheses load correctly in HTML and JSX designs without breaking CSS or JavaScript strings.
