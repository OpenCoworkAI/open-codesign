---
"@open-codesign/desktop": patch
---

Keep asynchronous clarification answers and file-field state bound to their request and session, so a cancelled upload cannot overwrite a later question with the same question ID.
