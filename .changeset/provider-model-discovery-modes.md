---
"@open-codesign/desktop": patch
"@open-codesign/i18n": patch
"@open-codesign/shared": patch
---

Formalize provider `modelDiscoveryMode` (`models`, `static-hint`, `manual`, `infer-only`) so listing, Settings pickers, imports, and diagnostics no longer treat GET `/models` as universal.
