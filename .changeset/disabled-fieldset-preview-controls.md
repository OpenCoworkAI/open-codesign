---
"@open-codesign/desktop": patch
---

Reject preview interactions with controls disabled by an enclosing fieldset before changing values or dispatching input. Preserve the native first-legend exception and existing inert and ARIA-disabled checks.
